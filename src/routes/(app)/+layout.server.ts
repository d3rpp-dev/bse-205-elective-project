// import type { LayoutServerLoad } from "./$types";

// export const load = (async (event) => {
// 	const user_cookie = event.cookies.get("session-token");

// 	return {};
// }) satisfies LayoutServerLoad;
